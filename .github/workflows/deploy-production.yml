name: Deploy Production

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual production promotion placeholder. Wire Vercel/Railway production tokens before enabling."

